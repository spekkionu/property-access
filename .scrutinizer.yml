filter:
    excluded_paths: [tests/*]
checks:
    php:
        code_rating: true
        duplication: true
tools:
    external_code_coverage: true
    php_mess_detector: true
    php_cpd: true
    php_pdepend:
        excluded_dirs: [tests]